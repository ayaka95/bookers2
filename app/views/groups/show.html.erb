<h1>グループ詳細</h1>
<p>グループ画像</p>
<%= @group.name %></br>
<%= @group.introduction %></br>
<% @group.users.each do |user| %>
  <li><%= user.name %></li>
<% end %>
<% if @group.is_owned_by?(current_user) %>
  <%= link_to "編集", edit_group_path(@group.id) %>
  <%#= link_to "イベントメッセージ", %>
<% end %>
<% if @group.include_user?(current_user) %>
  <%= link_to "グループを退会する", group_group_users_path(@group.id), method: 'delete' %>
<% else %>
  <%= link_to "グループに参加する", group_group_users_path(@group.id), method: 'post' %>
<% end %>